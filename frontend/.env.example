# API Backend URL (only needed for local development when frontend runs on different port)
# In production, frontend is served by backend, so relative URLs are used automatically
# For local dev with Vite dev server (port 5173) connecting to backend (port 8000):
VITE_API_URL=http://localhost:8000
